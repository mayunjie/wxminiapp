<view>
  <view class='tab'>
    <view class="{{currentTab==0 ? 'on' : ''}}" data-current="0" bindtap="swichNav">群通知</view>
    <view class="{{currentTab==1 ? 'on' : ''}}" data-current="1" bindtap="swichNav">我创建的</view>
  </view>
  
  <!-- 群内通知start -->
  <view wx:if='{{currentTab==0}}'>
    <view wx:if='{{groupData==""}}' style='color:gray;padding:40rpx 0;text-align:center;'>
      从群内打开该小程序才能查看到群通知
    </view>
    <view wx:for="{{groupData}}" data-noticeid="{{item.id}}" bindtap='gotoEnroll' class='item'>
      <view style='font-weight:bold;'>
        {{item.title}}
      </view>
      <text style='font-size:26rpx;color:#666;'>{{item.showTime}}</text>
    </view>
  </view>
  <!-- 群内通知end -->

  <!-- 我创建的通知start -->
    <view wx:if='{{currentTab==1}}'>
      <view wx:for="{{createData}}" data-noticeid="{{item.id}}" bindtap='gotoEnroll' class='item'>
        <view style='font-weight:bold;'>
          {{item.title}}
        </view>
        <text style='font-size:26rpx;color:#666;'>{{item.showTime}} </text>
      </view>
    </view>
  <!-- 我创建的通知end-->
  

  <view direction="all" class='creat' bindtap='creat'>
    创建通知
  </view>

</view>